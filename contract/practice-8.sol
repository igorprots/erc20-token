pragma solidity ^0.6.8;

contract MyContract {

    function foo() external {
        // if(msg.sender >= 100 && block.timestamp > 123124124124....||)

        if(! boolValue){
            //
        } else{

        // }
        for(uint i = 0; i < 10; i++) {
           i = 1......9
        }

        bool isOk = true;
        while(isOk) {
            if() {
                isOk = false;
                break;
                continue;
            }
        }
    }
}